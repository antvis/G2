/**
 * 所有 Geometry 的实现逻辑，包含：
 * 1. 图形语法数据处理、映射过程
 * 2. 自定义 Shape
 * 3. Geometry，Element 及 API
 * 4. 各种内置的 Geometry
 */

export { Geometry } from './geometry';
export { Element } from './element';

export { Interval, IntervalOptions } from './interval';
