import { getScale } from '@antv/scale';

export { getScale, ScaleConfig, Scale } from '@antv/scale';

export const Linear = getScale('linear');
export const Category = getScale('cat');
export const TimeCat = getScale('timeCat');
export const Identity = getScale('identity');
