import { StyleSheet, Theme } from '../types/theme';

/**
 * 使用样式表创建处理，这个方法中，包含了一个主题的模板结构
 * @param stylesheet
 * @returns
 */
export function createThemeByStyleSheet(styleSheet: Partial<StyleSheet>): Theme {
  return {};
}
