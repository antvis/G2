/**
 * 主题的方案，包含：
 * 1. token 约束
 * 2. 生成浅色、暗色两套内置主题
 */

import { PlainObject } from '../types';

/**
 * // todo 根据主题颜色，获取最后的 theme object
 * @param name
 * @returns
 */
export function getTheme(name: string): PlainObject {
  return {};
}
