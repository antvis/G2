import type { Constraint } from './constraint';

/**
 * 约束布局构建和计算
 */
export class Solver {
  
  /**
   * 添加多条约束
   * @param constraint
   */
  public addConstraint(...constraints: Constraint[]) {

  }

  /**
   * 计算返回布局
   */
  public calc() {

  }
}