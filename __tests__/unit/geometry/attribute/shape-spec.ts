import {
  Shape,
} from '../../../../src/geometry/attribute';

describe('attribute shape', () => {
  it('value', () => {
    expect(Shape).toBeDefined();
  });

  it('callback', () => {
    expect(Shape).toBeDefined();
  });
});
