import { Position } from '../../../../src/geometry/attribute';

describe('attribute position', () => {
  it('value', () => {
    expect(Position).toBeDefined();
  });

  it('callback', () => {
    expect(Position).toBeDefined();
  });
});
