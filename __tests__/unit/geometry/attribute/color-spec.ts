import {
  Color,
} from '../../../../src/geometry/attribute';

describe('attribute color', () => {
  it('value', () => {
    expect(Color).toBeDefined();
  });

  it('callback', () => {
    expect(Color).toBeDefined();
  });
});
