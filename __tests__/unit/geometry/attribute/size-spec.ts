import { Size } from '../../../../src/geometry/attribute';

describe('attribute color', () => {
  it('value', () => {
    expect(Size).toBeDefined();
  });

  it('callback', () => {
    expect(Size).toBeDefined();
  });
});
