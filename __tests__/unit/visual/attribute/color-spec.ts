import { Color } from '../../../../src/visual/attribute';

describe('attribute color', () => {
  it('value', () => {
    expect(Color).toBeDefined();
  });

  it('callback', () => {
    expect(Color).toBeDefined();
  });
});
