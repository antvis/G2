import { Shape } from '../../../../src/visual/attribute';

describe('attribute shape', () => {
  it('value', () => {
    expect(Shape).toBeDefined();
  });

  it('callback', () => {
    expect(Shape).toBeDefined();
  });
});
