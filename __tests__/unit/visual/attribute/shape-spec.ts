import { Shape } from '@g2/visual/attribute';

describe('attribute shape', () => {
  it('value', () => {
    expect(Shape).toBeDefined();
  });

  it('callback', () => {
    expect(Shape).toBeDefined();
  });
});
