import { Position } from '../../../../src/visual/attribute';

describe('attribute position', () => {
  it('value', () => {
    expect(Position).toBeDefined();
  });

  it('callback', () => {
    expect(Position).toBeDefined();
  });
});
