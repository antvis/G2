import { Size } from '../../../../src/visual/attribute';

describe('attribute color', () => {
  it('value', () => {
    expect(Size).toBeDefined();
  });

  it('callback', () => {
    expect(Size).toBeDefined();
  });
});
