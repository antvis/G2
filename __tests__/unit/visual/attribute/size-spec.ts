import { Size } from '@g2/visual/attribute';

describe('attribute color', () => {
  it('value', () => {
    expect(Size).toBeDefined();
  });

  it('callback', () => {
    expect(Size).toBeDefined();
  });
});
